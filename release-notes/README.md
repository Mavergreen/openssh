# Release notes

One file per release, named `<full-version>.md`, e.g. `9.9p2-mavericks.1.md`. Its contents
become the Sparkle appcast `<description>` and the GitHub Release body. A release with no
matching file gets a generated default.
